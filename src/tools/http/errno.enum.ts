////////////////////////////////////////////////////////////////////////////////
// 错误码:
// 这个需要和后端约定好, 具体情况具体分析
// 1. 错误码是直接用 http 状态码还是在响应体中中定义一个字段(如:errno/code)
// 2. 具体的值和对应的错误
////////////////////////////////////////////////////////////////////////////////
export enum ErrnoEnum {
  // 未登录/token过期
  Unauthorized = 401,

  // 没有权限/签名验证失败
  Forbidden = 403,

  // 数据验证失败
  FailedToValidate = 400,

  // 内部错误
  InternalError = 500,
}

